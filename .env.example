# Required: OpenAI API key for chat functionality
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your-openai-api-key

# Optional: Secret key for JWT token signing (defaults to insecure placeholder)
# Generate a secure random key: openssl rand -hex 32
# SECRET_KEY=your-secret-key-change-in-production

# Optional: Backend API base URL (defaults to http://localhost:8000)
# API_BASE_URL=http://localhost:8000

# Optional: JWT token expiration in minutes (defaults to 30)
# ACCESS_TOKEN_EXPIRE_MINUTES=30

# Optional: JWT algorithm (defaults to HS256)
# ALGORITHM=HS256
